# Baseline positional-discrepancy figures

These 12 PDFs were preserved from the validated pre-restructure analysis for final visual comparison. They are reference artifacts, not files generated by the current pipeline.

The current positional figures are generated under `output/positional_discrepancy/`, which is intentionally ignored by Git.
